// Loop through a long list
// scroll position
// Intersection observer
// Implementing DOM recycling
// Mutation observer

let
  root = document.getElementById("root"),
  list = document.getElementById("list");

console.log(root);
console.log(list);